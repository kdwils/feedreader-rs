name: Build Push Sign
on:
  push:
    branches: ["main"]
jobs:
  build-push-sign:
    uses: kdwils/homelab-workflow/.github/workflows/build-push-sign.yml@main
    secrets: inherit
    with:
      image: kdwils/feedreader
      registry: ghcr.io
  update-image-tag:
    needs: build-push-sign
    uses: kdwils/homelab-workflow/.github/workflows/update-image-tag.yml@main
    secrets: inherit
    with:
      image: kdwils/feedreader
      registry: ghcr.io
      deploy_repo: kdwils/feedreader
